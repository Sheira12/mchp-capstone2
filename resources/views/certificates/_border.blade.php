{{--
    Certificate border — rendered as inline SVG, no positioning.
    DomPDF treats inline SVG as block-level content with natural dimensions.
    We use height="0" on the wrapper to prevent adding document flow height.
--}}
